import React from "react";

class Form extends React.Component {
    render() {
        return (
            <div>
                Component with Form
            </div>
        );
    }
}

export default Form;